#include <Practica18.h>
#use standard_io(b)

void main()
{
   set_tris_b(0b00000001);
   while(TRUE)
   {
      output_high(PIN_B2);
      delay_ms(250);
      output_low(PIN_B2);
      delay_ms(250);
      
   }

}
